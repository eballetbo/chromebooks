metadata:
  name: bootrr
  description: "Chromebooks testing"
  environment:
    - lava-test-shell

run:
  steps:
    - export PATH="$PATH:$(pwd)/tools/testing"
    - timeout 1m test-cros-ec-spi-stress.sh cros-ec-spi-stress || true
